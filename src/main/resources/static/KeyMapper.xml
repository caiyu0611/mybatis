<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.njzhenghou.mapper.KeyMapper">

    <select id="getKeyById" parameterType="int" resultType="Key">
        select * from `key` where kid =#{kid};
    </select>

</mapper>